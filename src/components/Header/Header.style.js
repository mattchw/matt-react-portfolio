import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(theme => ({
  container: {
    backgroundColor: '#282c34',
  },
  containerItem: {
    paddingTop: '30vh',
  },
  link: {
    textDecoration: 'none',
  }
}));